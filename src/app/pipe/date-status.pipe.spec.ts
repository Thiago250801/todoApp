import { DateStatusPipe } from './date-status.pipe';

describe('DateStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DateStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
